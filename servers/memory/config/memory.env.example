# Memory System Database Configuration
# Copy this file to interfaces/model-context-protocol/servers/memory/config/mcp-memory.env
# and update with your database credentials

# PostgreSQL Connection Settings
MEMORY_DB_HOST=localhost
MEMORY_DB_PORT=5432
MEMORY_DB_NAME=memory_system
MEMORY_DB_USER=ai_assistant
MEMORY_DB_PASSWORD=your_secure_password_here

# Optional: Embedding Model Configuration
# EMBEDDING_MODEL=all-MiniLM-L6-v2  # Default: sentence-transformers model
# EMBEDDING_BATCH_SIZE=32            # Default: batch size for embedding generation

# Performance Settings (if using pgvectorscale)
# VECTOR_INDEX_TYPE=hnsw            # Options: hnsw, ivfflat, diskann
# VECTOR_INDEX_PARAMS={"m": 16, "ef_construction": 64}  # HNSW parameters
